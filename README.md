A simple React to-do list app that allows users to add, edit, and delete tasks.
Built using React state to manage the list dynamically and update the UI in real time.
Purpose: practice state management, event handling, and basic CRUD operations in React.
